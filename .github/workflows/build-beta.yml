name: Build_Beta_Copy_Test

on:
push:
branches:
- main

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - name: Checkout Source Repo
    uses: actions/checkout@v4

  - name: Clone Build Repo
    run: |
      git clone https://x-access-token:${{ secrets.BUILD_REPO_TOKEN }}@github.com/UmaTechspire/Build_Beta_BTG.git buildrepo

  - name: Verify Clone
    run: |
      ls -la
      ls -la buildrepo
```
